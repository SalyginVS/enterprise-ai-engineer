# M0: Foundations — Конспект

**Период:** фев 2026  
**Статус:** 🟡 В процессе  
**Цель модуля:** от нулевого опыта к рабочему пониманию LLM API и simple agents

---

## Ключевые концепции

### Что такое Ollama и почему это не просто "запускалка"

Ollama — это процесс-менеджер с HTTP API (Application Programming Interface — интерфейс для программного взаимодействия), который держит языковую модель загруженной в VRAM (Video RAM — видеопамять GPU) между запросами. Это критически важно: без этого каждый новый запрос тратил бы 5-30 секунд только на загрузку модели в память, прежде чем начать генерацию.

Аналогия: Ollama похож на веб-сервер (например, Nginx), только вместо статических файлов он "раздаёт" ответы языковой модели. Модель живёт в памяти, запросы приходят через HTTP (HyperText Transfer Protocol — протокол передачи данных), ответы уходят обратно.

**Два ключевых эндпоинта (endpoint — точка входа API):**
- `POST /api/generate` — одиночный запрос, нет истории диалога
- `POST /api/chat` — чатовый режим с историей сообщений (messages array)

### Почему qwen3:8b для обучения, а не сразу 32B

*[Заполнить после задания 0.1]*

---

## Задание 0.1: Первый вызов к LLM через Python

**Дата:** _  
**Модель:** qwen3:8b  
**Статус:** ⬜

### Что делал

*[Описание своими словами]*

### Что понял (инсайты)

*[Ключевые открытия, не очевидные заранее]*

### Грабли

*[Что не сработало сразу, почему, как решил]*

### Артефакт

→ `src/first_llm_call.py`

---

## Задание 0.2: Prompt Engineering

**Дата:** _  
**Статус:** ⬜

### Что делал

*[Описание]*

### Ключевые выводы по промптингу

*[Что реально работает, что нет]*

### Грабли

*[...]*

---

## Задание 0.3: Tool Use & Function Calling

**Дата:** _  
**Статус:** ⬜

### Концепция tool use

*[Объяснение своими словами: зачем нужен tool use, как работает цикл]*

### Грабли

*[...]*

---

## Задание 0.4: Simple Agent + LLM Gateway (M0→M1 мост)

**Дата:** _  
**Статус:** ⬜

### Что построил

*[Описание CustomerSupportAgent + заготовка шлюза]*

---

## Итоги модуля M0

### Что умею теперь (честно)

*[Заполнить в конце модуля]*

### Что осталось непонятным → вынести в M1

*[...]*

### Время на модуль

Плановое: 16-24 часа  
Фактическое: _ часов
